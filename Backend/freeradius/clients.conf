# FreeRADIUS Clients Configuration
# Dynamic clients loaded from database via SQL module (radius_nas table)
# Only localhost defined here for testing, all other NAS from database

client localhost {
    ipaddr = 127.0.0.1
    secret = testing123
    require_message_authenticator = no
    nas_type = other
}

# Kubernetes pod network - for backend health checks
client k8s_network {
    ipaddr = 10.0.0.0/8
    secret = testing123
    require_message_authenticator = no
    nas_type = other
}
